import { StylesToggleDirective } from "./styles-toggle.directive";

describe("StylesToggleDirective", () => {
  it("should create an instance", () => {
    const directive = new StylesToggleDirective();
    expect(directive).toBeTruthy();
  });
});
