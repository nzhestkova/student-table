import { PatternControlDirective } from "./pattern-control.directive";

describe("PatternErrorDirective", () => {
  it("should create an instance", () => {
    const directive = new PatternControlDirective();
    expect(directive).toBeTruthy();
  });
});
